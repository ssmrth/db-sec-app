{
  "name": "nosql-security-monitor",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "lsof -ti:8080 | xargs kill -9 2>/dev/null || true && node server.js",
    "dev": "lsof -ti:8080 | xargs kill -9 2>/dev/null || true && nodemon server.js",
    "usage": "node scripts/checkUsage.js",
    "kill-ports": "lsof -ti:8080 | xargs kill -9 2>/dev/null || true && lsof -ti:3002 | xargs kill -9 2>/dev/null || true",
    "frontend": "cd frontend && npm start",
    "frontend:install": "cd frontend && npm install",
    "frontend:build": "cd frontend && npm run build",
    "build": "npm run frontend:install && npm run frontend:build",
    "postinstall": "cd frontend && npm install",
    "demo": "bash ../start-demo.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "nosql",
    "security",
    "mongodb",
    "injection",
    "detection",
    "ai",
    "gemini"
  ],
  "author": "Security Team",
  "license": "MIT",
  "description": "Real-time NoSQL injection detection and AI-powered incident reporting system",
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "mongo-sanitize": "^1.1.0",
    "mongoose": "^8.13.2",
    "nodemailer": "^6.10.0",
    "pdfkit": "^0.16.0",
    "socket.io": "^4.8.1",
    "winston": "^3.17.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "socket.io-client": "^4.8.1"
  }
}
